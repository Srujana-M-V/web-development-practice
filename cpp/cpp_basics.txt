// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    int a=45,b=23;
    char c='h';
    float z=2.43;
    double o=23.44444;
    cout<<"the value of int is\n"<<a<<endl;
    cout<<"the value of int is\n"<<b<<endl;
    cout<<"the value of sum of int is\n"<<a+b<<endl;
    cout<<"the value of sub of int is\n"<<a-b<<endl;
    cout<<"the value of float is\n"<<z<<endl;
    cout<<"the value of char is\n"<<c<<endl;
    cout<<"the value of double is\n"<<o<<endl;
    cout<<"the value of sum of float and double is\n"<<z+o<<endl;
    return 0;
}


SUM OF 2 NUMBERS

#include<iostream>
using namespace std;
int main(){
    int a, b;
    cout<<"Enter the number: ";
    cin>>a;
    cout<<"Enter the number: ";
    cin>>b;
    cout<<"The sum of two numbers are: "<<a+b<<endl;
    return 0;
}



AREA OF RECTANGLE

#include<iostream>
using namespace std;
int main(){
    int length,width;
    cout<<"Enter the length of the retangle: ";
    cin>>length;
    cout<<"Enter the width of the retangle: ";
    cin>>width;
    cout<<"the area of the rectangle is; "<<length*width<<endl;
    return 0;
}

ADDING FULL NAME

#include<iostream>
using namespace std;
int main(){
    string length,width;
    cout<<"Enter the length of the retangle: ";
    cin>>length;
    cout<<"Enter the width of the retangle: ";
    cin>>width;
    cout<<"the area of the rectangle is; "<<length+width<<endl;
    return 0;
}


AGE IN 5 YEARS

#include <iostream>
#include <string>  // Include this library to work with strings
using namespace std;

int main() {
    string firstName, lastName;

    cout << "Enter your first name: ";
    cin >> firstName;

    cout << "Enter your last name: ";
    cin >> lastName;

    cout << "Full Name: " << firstName << " " << lastName << endl;

    return 0;
}


AVG OF 4 NUMBERS

#include<iostream>
using namespace std;
int main(){
    int a,b,c,d;
    cout<<"Enter a: ";
    cin>>a;
    cout<<"Enter b: ";
    cin>>b;
    cout<<"Enter c: ";
    cin>>c;
    cout<<"Enter d: ";
    cin>>d;
    cout<<"average is; "<<(a+b+c+d)/(4)<<endl;
    return 0;
}


USING OPERATORS


#include<iostream>
using namespace std;
int main(){
    // cpp considers 0 is false and 1 is true
    int a,b,c;
    cout<<"enter the value of a: ";
    cin>>a;
    cout<<"enter the value of b: ";
    cin>>b;
    cout<<"enter the value of c: ";
    cin>>c;
    cout<<"value of a==b is "<<(a==b)<<endl;
    cout<<"value of a<b is "<<(a<b)<<endl;
    cout<<"value of a>b is "<<(a>b)<<endl;
    cout<<"value of a!=b is "<<(a!=b)<<endl;
    cout<<"value of a<=b is "<<(a<=b)<<endl;
    cout<<"value of a>=b is "<<(a>=b)<<endl;
}



LOGICAL OPERATORS

#include<iostream>
using namespace std;
int main(){
    // cpp considers 0 is false and 1 is true
    int a,b,c;
    cout<<"enter the value of a: ";
    cin>>a;
    cout<<"enter the value of b: ";
    cin>>b;
    cout<<"enter the value of c: ";
    cin>>c;
    cout<<"value of a==b is "<<(a==b)<<endl;
    cout<<"value of a<b is "<<(a<b)<<endl;
    cout<<"value of a>b is "<<(a>b)<<endl;
    cout<<"value of a!=b is "<<(a!=b)<<endl;
    cout<<"value of a<=b is "<<(a<=b)<<endl;
    cout<<"value of a>=b is "<<(a>=b)<<endl;
    cout<<"the condition satisfies "<<((a==b) && (a<=b))<<endl;//AND OPERATOR IS USED
    cout<<"the condition satisfies "<<((a==b) && (a<b))<<endl;//AND OPERATOR IS USED
    cout<<"the condition satisfies "<<((a>=b) || (a<b))<<endl;//OR OPERATOR IS USED
    cout<<"the condition satisfies "<<((a==c) && (a<c))<<endl;//AND OPERATOR IS USED
    cout<<"the condition satisfies "<<((a<=c) || (c<b))<<endl;//OR OPERATOR IS USED
    cout<<"the condition satisfies "<<((a==b) && (c<b))<<endl;//AND OPERATOR IS USED
    cout<<"the condition satisfies "<<(!(a==b))<<endl;//NOT OPERATOR IS USED
    return 0;
}

/* FOR AND OPERATOR
a = 3 b = 5
if a==b && a<=b
result is 0 * 1
output will be 0




SIZE OF DATA TYPES

// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    // Write C++ code here
    int a;//integer type occupies 4 bytes
    float b;//float type occupies 4 bytes
    long double c;// long double occupies 16 bytes
    cout<<"enter value for A\n";
    cin>>a;
    cout<<"enter value for B\n";
    cin>>b;
    cout<<"enter value for c\n";
    cin>>c;
    
    cout<<"the size of b is :"<<sizeof(b);
    cout<<"\nthe size of a is :"<<sizeof(a);
    cout<<"\nthe size of c is :"<<sizeof(c);

    return 0;
}

#include <iostream>
using namespace std;
int main() {
   int a=3;
   float b=1.23;
   cout<<"the value of a is :"<<a;
   cout<<"\nthe value of b is :"<<b;
   cout<<"\nthe value of a+b is :"<<a+b;
   
   cout<<"\nthe size of a is; "<< sizeof(a);
   cout<<"\nthe size of b is; "<< sizeof(b);
   cout<<"\nthe size of sum is; "<< sizeof(a+b);
   return 0;
   
}


TYPECASTING

// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    /*
    TYPE CASTING = CONVERTING ONE DATA TYPE INTO OTHER*/
    int a,b;
    float c;
    cout<<"Enter the value for a:"<<endl;
    cin>>a;
    cout<<"Enter the value for b:"<<endl;
    cin>>b;
    cout<<"\nEnter the value for c:"<<endl;
    cin>>c;
    
    cout<<"converting value from int to float : "<<float(a)<<endl;
    cout<<"converting value from int to float: "<<float(b)<<endl;
    cout<<"converting value from float to int: "<<int(c)<<endl;
    
    cout<<"converting value from int to float: "<<a+float(b)<<endl;
    cout<<"converting value from int to float: "<<float(a)+float(b)<<endl;
    cout<<"converting value from int to float: "<<a+int(c)<<endl;
}

CONSTANTS CAN BE CHANGED

/* CONSTANTS in cpp onstants are used to make variables unchangeable after their initial assignment, not to allow multiple values for the same variable. If you make a variable a constant, you won’t be able to assign it a new value later. */

#include <iostream>
using namespace std;

int main() {
int a;
cout<<"enter the a value"<<endl;
cin>>a;
cout<<"enter the a value"<<endl;
cin>>a;


cout<<"the a value is "<<a<<endl; //this gives a value as 2nd input value
    return 0;
}


CONSTANTS CANNOT BE CHANGED

// CONSTANTS = when variable is at constant mode if we try to give another value for variable it gives an error
#include <iostream>
using namespace std;
int main() {
    const int a=12;
    cout<<a;
    int a=5;
    cout<<a;
    return 0;
}

manipulators

// Online C++ compiler to run C++ program online
#include <iostream>
#include<iomanip>
using namespace std;
int main() {
    int a=25;
    int b=4;
    int c=2345;
    int d=1.23;
    cout<<setw(4)<<a<<endl;
    cout<<setw(4)<<b<<endl;
    cout<<setw(4)<<c<<endl;
    cout<<setw(4)<<d<<endl;
    
    cout<<setw(3)<<a<<endl;
    cout<<setw(3)<<b<<endl;
    cout<<setw(3)<<c<<endl;
    cout<<setw(3)<<d<<endl;
    
    cout<<setw(8)<<a<<endl;
    cout<<setw(5)<<b<<endl;
    cout<<setw(6)<<c<<endl;
    cout<<setw(7)<<d<<endl;


    return 0;
}

OPERATOR PRECEDENCE


// OPERATOR PRECEDENCE
#include <iostream>
using namespace std;

int main() {
    int a=3;
    int b=7;
    int c=9;
    cout<<((a+b)*(c-a))<<endl;
    cout<<((a%b)-(c*a))<<endl;
    cout<<((a/b)*(b*c)-(b-a))<<endl;

    return 0;
}


IF AND ELSE 
// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    int a=15;
    if (a<10){
        cout<<a;
    }
    else {
        cout<<"you are not eligible";
    }

    return 0;
}

IF, ELSE IF, AND ESLE.

#include <iostream>
using namespace std;

int main() {
    int age;
    string name;
    
    // Prompt the user for input
    cout << "Enter your age: ";
    cin >> age;
    cout << "Enter your name: ";
    cin >> name;

    // Check if name is "srujana" and age is 19
    if (age == 19 && name == "srujana") {
        cout << "You are allowed to party!" << endl;
    } else if (age > 18) {
        cout << "You are allowed, but you're not Srujana!" << endl;
    }  else if (age > 18 && name!="srujana") {
        cout << "You are allowed, but you're not Srujana!" << endl;
    } else {
        cout << "You are not allowed to party!" << endl;
    }



#include<iostream>
using namespace std;
int main(){
    int runs;
    string name;
    char letter;
    float average;
    bool word;
    double strikerate;
    cout<<"Enter the name:";
    cin>>name;
    cout<<"total runs scored in last match by "<<name<<" : ";
    cin>>runs;
    cout<<"First letter of "<<name<<" : ";
    cin>>letter;
    cout<<"Äverage in one day international: ";
    cin>>average;
    cout<<"strikerate in last match: ";
    cin>>strikerate;
    cout<<"name:"<<name;
    cout<<"\ntotal runs scored in last match by "<<name<<" : "<<runs;
    cout<<"\nFirst letter of "<<name<<" : "<<letter;
    cout<<"\nÄverage in one day international: "<<average;
    cout<<"\nstrikerate in last match: "<<strikerate;
    return 0;
}

